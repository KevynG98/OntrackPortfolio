import React, { useState } from 'react'

const ImageList = () => {
    const [selectdIndez, setSelectedIndex] = useState(0);
    const [selectedImage, setSelectedImage] = useState();

    return (
        <div>ImageList</div>
    )
}

export default ImageList